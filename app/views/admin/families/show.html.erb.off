<% content_for(:title, 'Familia') %>
<% breadcrumb(title) unless admin.singular? %>

<!--
  <button
      name="button" type="submit" class="btn btn-success"
      style="transition-property: -color, -background-color, -border-color,
    box-shadow !important; background-color: rgb(16, 35, 16) !important; color:
    rgb(232, 229, 227) !important; border-color: rgb(16, 35, 16)
  !important;"><span class="btn-label">Enregistrer</span></button>
-->


<% toolbar(:primary) do |t| %>

  <button class="btn btn-success" >
    <span class="btn-label">
      <%= link_to('Modifier', edit_families_admin_path(instance), class: 'bt-show-btn') %>
    </span>
  </button>

<% end %>

<% toolbar(:secondary) do |t| %>
  <%= t.delete %>
<% end %>

<p>todo</p>
